type: object
properties:
  organizationID:
    type: string
  organizationName:
    type: string
  bucketID:
    type: string
  bucketName:
    type: string
  shardID:
    type: integer
required: [organizationID, organizationName, bucketID, bucketName, shardID]
