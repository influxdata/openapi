type: object
properties:
  organizationID:
    type: string
  organizationName:
    type: string
  bucketID:
    type: string
  bucketName:
    type: string
  defaultRetentionPolicy:
    type: string
  retentionPolicies:
    $ref: "./RetentionPolicyManifests.yml"
required: [organizationID, organizationName, bucketID, bucketName, defaultRetentionPolicy, retentionPolicies]
