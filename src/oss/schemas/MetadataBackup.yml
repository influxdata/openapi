type: object
properties:
  boltdb:
    type: string
    format: binary
  sqlite:
    type: string
    format: binary
  shards:
    $ref: "./ShardMetadataManifest.yml"
required: [boltdb, sqlite, shards]
