description: The full response sent to end user when a function is invoked using http
allOf:
  - $ref: './FunctionHTTPResponseNoData.yml'
  - type: object
    properties:
      data:
        $ref: './FunctionHTTPResponseData.yml'
