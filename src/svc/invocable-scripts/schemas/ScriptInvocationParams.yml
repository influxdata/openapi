type: object
properties:
  params:
    type: object
    additionalProperties: true
    description: |
      Script parameters.
      Provide key-value pairs within this params object for parameters
      referenced in script.
