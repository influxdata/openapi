description: The full response sent to end user when a script is invoked using http
properties:
  type:
    type: string
    enum: [http]
  dataType:
    type: string
    enum: [json]
  headers:
    type: object
  statusCode:
    type: string
    enum: [200, 500, 404]
