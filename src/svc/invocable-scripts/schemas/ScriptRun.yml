description: The record that is kept of a script run, does not include data returned to user
allOf:
  - $ref: './ScriptRunBase.yml'
  - type: object
    properties:
      response:
        $ref: './ScriptHTTPResponseNoData.yml'
