description: The full response sent to end user when a script is invoked using http
allOf:
  - $ref: './ScriptHTTPResponseNoData.yml'
  - type: object
    properties:
      data:
        $ref: './ScriptHTTPResponseData.yml'
