description: The full response sent to end user when a function is invoked
allOf:
  - $ref: './FunctionRunBase.yml'
  - type: object
    properties:
      response:
        $ref: './FunctionHTTPResponse.yml'
