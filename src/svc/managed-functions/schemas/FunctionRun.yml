description: The record that is kept of a function run, does not include data returned to user
allOf:
  - $ref: './FunctionRunBase.yml'
  - type: object
    properties:
      response:
        $ref: './FunctionHTTPResponseNoData.yml'
