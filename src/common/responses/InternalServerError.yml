  # This represents the body of an 500 error.
  description: |
    Internal server error.
    The server encountered an unexpected situation.
  content:
    application/json:
      schema:
        $ref: "../schemas/Error.yml"
