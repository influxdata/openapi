get:
  operationId: GetPing
  summary: Get the status of the instance
  description: |
    Returns the status and InfluxDB version of the instance.

    Use this endpoint to monitor instance uptime.

    #### InfluxDB Cloud
    - Isn't versioned and doesn't return `X-Influxdb-Version` in the headers.
  servers:
    - url: ''
  tags:
    - Ping
    - System information endpoints
  responses:
    '204':
      description: |
        Success.
        Headers contain InfluxDB version information.
      headers:
        X-Influxdb-Build:
          schema:
            type: string
          description: |
            The type of InfluxDB build.
        X-Influxdb-Version:
          schema:
            type: integer
          description: |
            The version of InfluxDB.

            #### InfluxDB Cloud
              - Doesn't return version.
head:
  operationId: HeadPing
  summary: Get the status of the instance
  description: |
    Returns the status and InfluxDB version of the instance.

    Use this endpoint to monitor instance uptime.

    #### InfluxDB Cloud
    - Isn't versioned and doesn't return `X-Influxdb-Version` in the headers.
  servers:
    - url: ''
  tags:
    - Ping
  responses:
    '204':
      description: |
        Success.
        Headers contain InfluxDB version information.
      headers:
        X-Influxdb-Build:
          schema:
            type: string
          description: The type of InfluxDB build.
        X-Influxdb-Version:
          schema:
            type: integer
          description: |
            The version of InfluxDB.

            #### InfluxDB Cloud
            - Doesn't return version.
