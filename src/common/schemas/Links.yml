  type: object
  description: |
    TODO figure out what these links are used for
  properties:
    next:
      $ref: "./Link.yml"
    self:
      $ref: "./Link.yml"
    prev:
      $ref: "./Link.yml"
  required: [self]
