  type: array
  description: |
    A list of retention rules that expire or retain data.

    #### InfluxDB Cloud

    - `retentionRules` is required.

    #### InfluxDB OSS

    - `retentionRules` is optional.
  items:
    $ref: "./RetentionRule.yml"
