properties:
  isAvailable:
    description: determines if the cluster is available
    nullable: false
    readOnly: true
    type: string
  isBeta:
    description: determines if the cluster is in beta
    nullable: false
    readOnly: true
    type: string
  priority:
    description: priority for sorting the cluster
    nullable: false
    readOnly: true
    type: string
  providerId:
    description: the id of the cloud provider
    nullable: false
    readOnly: true
    type: string
  regionId:
    description: the id of the region
    nullable: false
    readOnly: true
    type: string
  regionName:
    description: the descriptive name of the region
    nullable: false
    readOnly: true
    type: string
