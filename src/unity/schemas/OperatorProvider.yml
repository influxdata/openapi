properties:
  provider:
    type: string
    description: name of the provider
  title:
    type: string
    description: title of the provider
  isAvailable:
    type: boolean
    description: the availability of the provider
required: [provider, title, isAvailable]
