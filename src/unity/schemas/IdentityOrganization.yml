properties:
  id:
    type: string
    description: idpe ID of the org
  name:
    type: string
    description: name of the org
  clusterHost:
    type: string
    description: the url of the current cluster
required:
  - id
  - name
  - clusterHost
