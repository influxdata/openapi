type: array
items:
  allOf:
    - $ref: './Organization.yml'
    - properties:
        account:
          $ref: './RelatedAccount.yml'
        billingContact:
          $ref: './BillingContact.yml'
          description: billing contact for the account
    - required: [account, billingContact]
