get:
  operationId: GetPaymentForm
  tags:
    - PaymentGateway
  responses:
    '200':
      description: A CreditCard Form parameter object
      content:
        application/json:
          schema:
            $ref: '../schemas/CreditCardParams.yml'
    default:
      description: Unexpected error
      $ref: '../../common/responses/ServerError.yml'
