get:
  operationId: GetUsageVectors
  tags:
    - Usage
  responses:
    '200':
      description: Usage vectors for the account
      content:
        application/json:
          schema:
            $ref: '../schemas/UsageVectors.yml'
    default:
      description: Unexpected error
      $ref: '../../common/responses/ServerError.yml'
