get:
  operationId: GetCloudSignout
  summary: Signout the current UI session
  tags:
    - Signout
  description: Signs out the current user from UI session.
  responses:
    "200":
      description: User signed out
    "401":
      description: Unauthorized access
      content:
        application/json:
          schema:
            $ref: '../../common/responses/ServerError.yml'
    default:
      description: Unsuccessful session expiry
      content:
        application/json:
          schema:
            $ref: '../../common/responses/ServerError.yml'
