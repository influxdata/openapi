  type: object
  properties:
    user:
      $ref: './Identity.yml'
    org:
      $ref: './Identity.yml'
    config:
      $ref: './OrgConfig.yml'
    returnToken:
      type: bool
      description: if true will return the auth token in the response
  required:
    - username
    - org
    - config
