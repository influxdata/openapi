  type: object
  properties:
    user:
      $ref: './Identity.yml'
    orgID:
      type: string
      description: organization id to add user to
    role:
      $ref: './Role.yml'
  required:
    - user
    - orgID
    - role
